<h4>
  Donate to <%= @organization.name %>
</h4>

<%= render 'charges' %>


<p>
  <%= link_to "Edit Organization", edit_organization_path(@organization), class: 'btn btn-warning btn-sm' %>
</p>

<p>
  <%= link_to "Delete", organization_path(@organization), data: {confirm: 'Delete?', method: :delete}, class: 'btn btn-danger btn-xs' %>
</p>
